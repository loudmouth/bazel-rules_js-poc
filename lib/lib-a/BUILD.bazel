load("@npm//@bazel/typescript:index.bzl", "ts_config", "ts_project")

# load("//tools/rules/eslint:defs.bzl", "eslint_test")

package(default_visibility = ["//visibility:public"])

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
    deps = [
        "//:tsconfig.json",
        ":tsconfig.json",
    ],
)


ts_project(
    name = "lib-a",
    srcs = [
        "index.ts",
    ],
    composite = True,
    tsconfig = ":tsconfig",
    deps = [
        "@npm//:node_modules",
    ],
)
